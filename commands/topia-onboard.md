---
name: topia-onboard
description: "Auto-generate project context for AI sessions. Use when starting on a new repo for the first time, or when CLAUDE.md / .topia/ context is missing or stale. Scans codebase and creates the setup so every future session starts with full context."
disable-model-invocation: true
---

# /topia-onboard

User-facing alias for the Topia **onboard** skill (`/topia:onboard`).

1. Invoke the Skill tool with `topia:onboard`
2. Follow the full workflow defined in that skill — do not shortcut steps
3. Announce: "Routing to topia:onboard (L2, sonnet)"

Also reachable via `/topia onboard` or `/topia:onboard`.
