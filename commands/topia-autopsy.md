---
name: topia-autopsy
description: "Full codebase health assessment. Use when diagnosing project health or starting a rescue workflow on legacy code. Analyzes complexity, dependencies, dead code, tech debt, and git hotspots. Produces a health score and rescue plan."
disable-model-invocation: true
---

# /topia-autopsy

User-facing alias for the Topia **autopsy** skill (`/topia:autopsy`).

1. Invoke the Skill tool with `topia:autopsy`
2. Follow the full workflow defined in that skill — do not shortcut steps
3. Announce: "Routing to topia:autopsy (L2, opus)"

Also reachable via `/topia autopsy` or `/topia:autopsy`.
