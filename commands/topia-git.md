---
name: topia-git
description: "Specialized git operations — semantic commits, PR descriptions, branch management, conflict resolution guidance. Replaces ad-hoc git commands with a dedicated, convention-aware utility."
disable-model-invocation: true
---

# /topia-git

User-facing alias for the Topia **git** skill (`/topia:git`).

1. Invoke the Skill tool with `topia:git`
2. Follow the full workflow defined in that skill — do not shortcut steps
3. Announce: "Routing to topia:git (L3, haiku)"

Also reachable via `/topia git` or `/topia:git`.
