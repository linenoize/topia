---
name: topia-docs
description: "Auto-generate and maintain project documentation. Creates README, API docs, architecture docs, changelogs, and keeps them in sync with code changes. The \\\"docs are never outdated\\\" skill."
disable-model-invocation: true
---

# /topia-docs

User-facing alias for the Topia **docs** skill (`/topia:docs`).

1. Invoke the Skill tool with `topia:docs`
2. Follow the full workflow defined in that skill — do not shortcut steps
3. Announce: "Routing to topia:docs (L2, sonnet)"

Also reachable via `/topia docs` or `/topia:docs`.
