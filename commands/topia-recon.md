---
name: topia-recon
description: "Fast codebase scanner. Use when any skill needs codebase context. Finds files, patterns, dependencies, project structure. Pure read-only — never modifies files."
disable-model-invocation: true
---

# /topia-recon

User-facing alias for the Topia **recon** skill (`/topia:recon`).

1. Invoke the Skill tool with `topia:recon`
2. Follow the full workflow defined in that skill — do not shortcut steps
3. Announce: "Routing to topia:recon (L2, haiku)"

Also reachable via `/topia recon` or `/topia:recon`.
