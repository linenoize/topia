---
name: topia-neural-memory
description: "Cross-session cognitive persistence via Neural Memory MCP. Captures decisions, patterns, errors, and insights with rich semantic links. Provides recall, hypothesis tracking, and evidence-based reasoning across projects."
disable-model-invocation: true
---

# /topia-neural-memory

User-facing alias for the Topia **neural-memory** skill (`/topia:neural-memory`).

1. Invoke the Skill tool with `topia:neural-memory`
2. Follow the full workflow defined in that skill — do not shortcut steps
3. Announce: "Routing to topia:neural-memory (L3, haiku)"

Also reachable via `/topia neural-memory` or `/topia:neural-memory`.
