---
name: topia-debug
description: "Root cause analysis for bugs and unexpected behavior. Traces errors through code, uses structured reasoning, and hands off to fix when cause is found. Core of the debug↔fix chain."
disable-model-invocation: true
---

# /topia-debug

User-facing alias for the Topia **debug** skill (`/topia:debug`).

1. Invoke the Skill tool with `topia:debug`
2. Follow the full workflow defined in that skill — do not shortcut steps
3. Announce: "Routing to topia:debug (L2, sonnet)"

Also reachable via `/topia debug` or `/topia:debug`.
