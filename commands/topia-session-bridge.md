---
name: topia-session-bridge
description: "Universal context persistence across sessions. Auto-saves decisions, conventions, and progress to .topia/ files. Loads state at session start. Use when any skill makes architectural decisions or establishes patterns that must survive session boundaries."
disable-model-invocation: true
---

# /topia-session-bridge

User-facing alias for the Topia **session-bridge** skill (`/topia:session-bridge`).

1. Invoke the Skill tool with `topia:session-bridge`
2. Follow the full workflow defined in that skill — do not shortcut steps
3. Announce: "Routing to topia:session-bridge (L3, haiku)"

Also reachable via `/topia session-bridge` or `/topia:session-bridge`.
