---
name: topia-doc-processor
description: "Generate and parse office documents — PDF, DOCX, XLSX, PPTX, CSV. Use when creating reports, exporting tabular data, or processing uploaded office files. NOT for project documentation (use docs)."
disable-model-invocation: true
---

# /topia-doc-processor

User-facing alias for the Topia **doc-processor** skill (`/topia:doc-processor`).

1. Invoke the Skill tool with `topia:doc-processor`
2. Follow the full workflow defined in that skill — do not shortcut steps
3. Announce: "Routing to topia:doc-processor (L3, sonnet)"

Also reachable via `/topia doc-processor` or `/topia:doc-processor`.
