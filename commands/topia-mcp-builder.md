---
name: topia-mcp-builder
description: "Build Model Context Protocol servers from specifications. Use when creating an MCP server for a tool, resource, or service that AI agents should access. Generates tool definitions, resource handlers, and test suites in TypeScript or Python (FastMCP)."
disable-model-invocation: true
---

# /topia-mcp-builder

User-facing alias for the Topia **mcp-builder** skill (`/topia:mcp-builder`).

1. Invoke the Skill tool with `topia:mcp-builder`
2. Follow the full workflow defined in that skill — do not shortcut steps
3. Announce: "Routing to topia:mcp-builder (L2, sonnet)"

Also reachable via `/topia mcp-builder` or `/topia:mcp-builder`.
