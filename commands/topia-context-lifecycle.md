---
name: topia-context-lifecycle
description: "Automated context boundary orchestration. Runs at phase ends, pre/post-compact, and after git push. Chunks work across sessions via session-bridge, context-pack, and neural-memory without losing continuity."
disable-model-invocation: true
---

# /topia-context-lifecycle

User-facing alias for the Topia **context-lifecycle** skill (`/topia:context-lifecycle`).

1. Invoke the Skill tool with `topia:context-lifecycle`
2. Follow the full workflow defined in that skill — do not shortcut steps
3. Announce: "Routing to topia:context-lifecycle (L3, haiku)"

Also reachable via `/topia context-lifecycle` or `/topia:context-lifecycle`.
