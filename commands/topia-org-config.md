---
name: topia-org-config
description: "Team org policy setup for Topia. Use during /topia finalize or when configuring .topia/org/org.md — asks structured questions and writes team roles, policies, approval flows, and governance level so guardian and readiness enforce your rules."
disable-model-invocation: true
---

# /topia-org-config

User-facing alias for the Topia **org-config** skill (`/topia:org-config`).

1. Invoke the Skill tool with `topia:org-config`
2. Follow the full workflow defined in that skill — do not shortcut steps
3. Announce: "Routing to topia:org-config (L2, sonnet)"

Also reachable via `/topia org-config` or `/topia:org-config`.
