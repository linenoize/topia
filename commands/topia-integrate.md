---
name: topia-integrate
description: "Clone, port, or convert features from any GitHub repo into your project. Use when stealing patterns from external repos or porting proven code. Understand before copy, challenge before implement. 4 modes: port (rewrite), compare (analysis), copy (transplant), improve (copy + optimize)."
disable-model-invocation: true
---

# /topia-integrate

User-facing alias for the Topia **integrate** skill (`/topia:integrate`).

1. Invoke the Skill tool with `topia:integrate`
2. Follow the full workflow defined in that skill — do not shortcut steps
3. Announce: "Routing to topia:integrate (L2, sonnet)"

Also reachable via `/topia integrate` or `/topia:integrate`.
