---
name: topia-db
description: "Database workflow specialist. Generates migration files with rollback scripts, detects breaking schema changes, and validates query parameterization."
disable-model-invocation: true
---

# /topia-db

User-facing alias for the Topia **db** skill (`/topia:db`).

1. Invoke the Skill tool with `topia:db`
2. Follow the full workflow defined in that skill — do not shortcut steps
3. Announce: "Routing to topia:db (L2, sonnet)"

Also reachable via `/topia db` or `/topia:db`.
