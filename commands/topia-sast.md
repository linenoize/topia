---
name: topia-sast
description: "Static analysis tool runner. Wraps ESLint, Semgrep, Bandit, Clippy, and language-specific analyzers with unified severity output. Use when deeper code analysis needed beyond pattern matching."
disable-model-invocation: true
---

# /topia-sast

User-facing alias for the Topia **sast** skill (`/topia:sast`).

1. Invoke the Skill tool with `topia:sast`
2. Follow the full workflow defined in that skill — do not shortcut steps
3. Announce: "Routing to topia:sast (L3, haiku)"

Also reachable via `/topia sast` or `/topia:sast`.
