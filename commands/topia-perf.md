---
name: topia-perf
description: "Performance regression gate. Detects N+1 queries, sync-in-async, missing indexes, memory leaks, and bundle bloat before they reach production."
disable-model-invocation: true
---

# /topia-perf

User-facing alias for the Topia **perf** skill (`/topia:perf`).

1. Invoke the Skill tool with `topia:perf`
2. Follow the full workflow defined in that skill — do not shortcut steps
3. Announce: "Routing to topia:perf (L2, sonnet)"

Also reachable via `/topia perf` or `/topia:perf`.
